#' Multiply together two numbers
#'
#' @param x A number
#' @param y A number
#' @return The product of \code{x} and \code{y}
#' @examples
#' times(1, 1)
#' times(10, 1)
times <- function(x, y) {
    x * y
}
